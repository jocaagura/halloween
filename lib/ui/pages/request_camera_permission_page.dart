import 'package:flutter/material.dart';


class RequestCameraPermissionPage extends StatelessWidget {
  const RequestCameraPermissionPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
